@model IEnumerable<WebEstacionamentoTcc20.Models.GrupoAcesso>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UsuarioEst.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmpresaId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UsuarioEst.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmpresaId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GrupoAcessoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GrupoAcessoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GrupoAcessoId })
        </td>
    </tr>
}

</table>
